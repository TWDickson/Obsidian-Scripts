# TTasks Bugs

- Due Date Estimation not cleared by "Clear" button (awkward to switch back and forth)
- Add Dependency... list option should float other project tasks to the top of the list
- Add Dependency Verbiage makes me think I'm adding something that depends on this task, maybe we need to think about that a bit more. 
- Graph Dependency Layout should allow creating tasks branching from another task, I'm thinking something like a clickable plus icon which would create another task that depends on the target
- Graph Dependency View Rows should be defined by a project, we made them a bit tighter which is good but a stricter group of rows makes picking out specific projects easier, projects could even have boarders with a nice text in the top left with the project title. Items can still depend on other project's items.
- Does changing a task's status to and from 'Completed' update the task properly?
